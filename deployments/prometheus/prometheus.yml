global:
  scrape_interval: 15s

scrape_configs:
  - job_name: fulfillhub-api
    authorization:
      type: Bearer
      credentials: local-metrics-token
    static_configs:
      - targets:
          - api:8080
