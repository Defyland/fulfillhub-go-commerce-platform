global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/rules/*.yml

scrape_configs:
  - job_name: fulfillhub-api
    authorization:
      type: Bearer
      credentials: local-metrics-token
    static_configs:
      - targets:
          - api:8080
