name: project-quality

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  baseline:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Validate repository baseline
        run: ./scripts/validate_phase0.sh
      - name: Validate shell scripts
        run: sh -n scripts/validate_phase0.sh scripts/validate_production_readiness.sh scripts/run_compose_profile.sh scripts/run_compose_saga_smoke.sh

  production-readiness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Validate production readiness pack
        run: ./scripts/validate_production_readiness.sh

  go:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: Formatting check
        run: test -z "$(gofmt -l cmd internal)"
      - name: Vet
        run: go vet ./...
      - name: Unit and request tests
        run: go test ./...
      - name: Coverage
        run: |
          go test -coverprofile=coverage.out ./...
          go tool cover -func=coverage.out | tee coverage.txt
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: go-coverage
          path: |
            coverage.out
            coverage.txt
      - name: Race detector
        run: go test -race ./...
      - name: Vulnerability scan
        run: go run golang.org/x/vuln/cmd/govulncheck@v1.3.0 ./...
      - name: Native benchmark smoke
        run: go test -bench=. -run '^$' -benchtime=1x ./internal/api

  postgres-integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: fulfillhub
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: fulfillhub_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: PostgreSQL integration tests
        env:
          DATABASE_URL: postgres://fulfillhub:postgres@localhost:5432/fulfillhub_test?sslmode=disable
        run: go test ./internal/postgres -run TestPostgresStoreIntegration -count=1

  rabbitmq-integration:
    runs-on: ubuntu-latest
    services:
      rabbitmq:
        image: rabbitmq:3.13-management-alpine
        ports:
          - 5672:5672
        options: >-
          --health-cmd "rabbitmq-diagnostics -q ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: go.mod
      - name: RabbitMQ integration tests
        env:
          RABBITMQ_URL: amqp://guest:guest@localhost:5672/
        run: go test ./internal/messaging -run TestRabbitPublisherIntegration -count=1

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate Docker build
        run: docker build .
      - name: Validate Compose config
        run: docker compose config

  k6:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate k6 scripts are present
        run: test -f benchmarks/k6/smoke.js && test -f benchmarks/k6/load.js && test -f benchmarks/k6/stress.js && test -f benchmarks/k6/spike.js
      - name: Validate benchmark budgets
        run: ./scripts/validate_benchmark_budgets.py

  compose-profile-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Prepare k6 wrapper
        run: |
          mkdir -p .ci-bin
          cat > .ci-bin/k6 <<'EOF'
          #!/usr/bin/env sh
          exec docker run --rm --network host \
            -e BASE_URL \
            -e K6_SUMMARY_EXPORT \
            -v "$PWD:/work" \
            -w /work \
            grafana/k6:0.54.0 "$@"
          EOF
          chmod +x .ci-bin/k6
      - name: Run Compose smoke profile
        env:
          SCENARIOS: smoke
          RESULT_ROOT: benchmarks/results/ci
        run: PATH="$PWD/.ci-bin:$PATH" ./scripts/run_compose_profile.sh
      - name: Upload Compose profile artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compose-profile-smoke
          path: benchmarks/results/ci/

  compose-saga-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Compose saga smoke
        run: ./scripts/run_compose_saga_smoke.sh

  supply-chain:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
    steps:
      - uses: actions/checkout@v4
      - name: Build image for scan
        run: docker build -t fulfillhub-ci:latest .
      - name: Generate SBOM
        uses: anchore/sbom-action@v0
        with:
          image: fulfillhub-ci:latest
          artifact-name: fulfillhub-sbom.spdx.json
      - name: Scan repository filesystem
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          scan-type: fs
          scan-ref: .
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: "1"
      - name: Scan container image
        uses: aquasecurity/trivy-action@v0.36.0
        with:
          image-ref: fulfillhub-ci:latest
          severity: HIGH,CRITICAL
          ignore-unfixed: true
          exit-code: "1"

  markdown:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DavidAnson/markdownlint-cli2-action@v20
        with:
          globs: |
            **/*.md

  openapi:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: stoplightio/spectral-action@v0.8.6
        with:
          file_glob: openapi.yaml

  secrets:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
